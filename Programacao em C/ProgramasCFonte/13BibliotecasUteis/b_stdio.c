#include <stdio.h>

/* Funções de entrada e saída
 * fclose(), fopen(), fgets(), fflush()
 * fscanf(), printf(), scanf()
 * gets(), getchar() fgets(), getc()
 * feof()
 * FILE, EOF, NULL
 */
int main() {
	return 0;
}
